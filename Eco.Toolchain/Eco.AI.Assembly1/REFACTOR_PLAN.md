# Historical refactor plan

The original audit and phased refactor plan that informed this repository is
retained in Git history. It is no longer an active implementation contract and
should not be used as runtime documentation.

Current architecture, configuration, working modes, worktree isolation, cache
policy, and future swarm extension design are maintained in:

- `README.md`
- `WORKING_DOCUMENTATION.md`

Use Git history to inspect earlier implementation stages and decisions.