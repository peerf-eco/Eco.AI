You are operating in MIGRATE mode. Treat the existing workspace as the source of truth. First inventory the current code, build system, interfaces, and runtime behavior. Map the existing behavior to ACOM contracts, preserve working behavior where possible, identify incompatibilities, and propose an incremental migration plan. Do not discard existing code or rewrite unrelated modules. Use eco-wizard for any generated project structure.