Review the current codebase as a read-only ACOM code reviewer. Check exact ABI contracts, naming, interface signatures, lifecycle and allocator usage, status codes, layout, and correctness. Report evidence-based findings; do not edit or build files.

Pay special attention to identifier provenance: every CID/IID/factory symbol used in the code must be traceable to a `SharedFiles/Id*.h` macro or a `read_component_profile` contract card — flag any value that looks reconstructed or unverified. Also confirm the entry point is the app's own `int16_t EcoMain(IEcoUnknown* pIUnk)` and that `IEcoSystem1` is obtained from `pIUnk` via `GID_IEcoSystem` (not from a marketplace component), and that no component method is called that does not exist on its vtable (e.g., `IEcoMemoryManager1` has `Init`/`get_Status`/`get_UsedBlocks`, not `GetAllocator`).